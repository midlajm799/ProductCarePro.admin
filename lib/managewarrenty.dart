import 'package:flutter/material.dart';

class ManageWarranty extends StatelessWidget {
  const ManageWarranty({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Warranty",style: TextStyle(
        fontSize: 50),)
    );
  }
}
